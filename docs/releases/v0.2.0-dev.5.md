# v0.2.0-dev.5

修复 Cloudflare Access 登录后无法进入管理后台的问题：Access 公钥读取独立直连 Cloudflare，不受 Webshare 节点配置影响。

完整保留 JWT 签名、签发方、受众、有效期校验，以及请求超时、响应大小限制和禁止跟随重定向的边界。Codex 流量继续使用已选择的住宅代理。

已有管理员口令、账号、代理配置和持久数据保持不变。Node 安装包升级沿用现有环境文件与数据目录。Worker 用户使用现有配置部署，保留 ACCOUNT 和 ADMIN_API_KEY；不要重新执行旧的密钥迁移流程。

验证与发布证据：[ACCESS-PROXY-001](../maintenance/ACCESS-PROXY-001.md)。
