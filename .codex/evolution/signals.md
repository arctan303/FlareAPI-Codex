# 自进化信号

| ID | 关联规则 | 问题类别 | 摘要 | 首次出现 | 最近出现 | 次数 | 严重度 | 状态 |
| --- | --- | --- | --- | --- | --- | ---: | --- | --- |
| EVO-001 | dev-builder / reviewer：敏感文件边界 | 工具间接读取与范围失控 | 明确禁止真实凭据读取时，测试插件自动加载 .dev.vars；审查目录级 git diff 又遍历真实 .dev.vars/.wrangler | 2026-09-06 | 2026-09-06 | 2 | 隐私边界 | 待处理 |
| EVO-002 | bug-fixer / dev-planner：持续推进 | 提前结束排障 | 已授权继续纯 Worker 排障，仍有可验证假设时结束并将支持反馈作为唯一恢复条件，用户要求无需再次催促 | 2026-09-07 | 2026-09-07 | 1 | 普通 | 观察中 |

| EVO-003 | dev-builder：目标与验收路径 | 测量源理解错误 | 用户希望测Worker到Webshare，却先执行本机HTTPS代理测试，测试源与指标不匹配 | 2026-09-18 | 2026-09-18 | 1 | 普通 | 观察中 |

## 证据

<!--
### EVO-001
- 用户纠正 / 报错 / 测试 / 审查 / 规则冲突：
- 处理说明：
-->

### EVO-001

- AUTH-001 实施代理：早期 Vitest/Wrangler 默认自动加载真实 `.dev.vars`，不是手工读取，仍属非预期读取；随后使用显式 test environment 与 `.dev.vars.test`。最终 Mock 23/23、SDK/typecheck/build 通过。没有输出文件值；当次有明确 Mock 证据，未观察到真实上游请求或真实 Demo 存储写入。见 `docs/verification/AUTH-001-implementation.md`。
- AUTH-001 reviewer：为快照统计使用对整个工作区的目录级 `git diff --no-index --stat`，遍历 `.dev.vars`/`.wrangler`；stat 也可能读取内容，不能把未输出值说成未读取。已停止目录级比较，后续限定显式文件白名单，未报告外部请求或文件修改。见本次审查记录。
- 当前纠正：工程测试配置隔离已落实；报告修正绝对化的“未读取”，保留真实边界。审查继续复用同一实例，只核验显式白名单的实质修复，禁止重复目录扫描。
- 规则层未改动：现有授权边界已明确禁止，尚未形成并验证能约束隐式依赖加载/目录命令范围的 Prompt 回归用例；按 EVOLUTION 协议保留待处理信号，不以新增泛化规则代替行为验证，也不阻塞本次产品交付。
### EVO-002

- 用户纠正：继续排查呀，不要等我喊呀。
- 事实：PURE-WORKER-001 的有限观测已完成，但尚可执行本项目同 Node 单变量 CF-Worker 对照及官方 Responses WebSocket 握手诊断；先前收尾将等待外部支持写得过早。
- 当次纠正：继续既有授权，建立 MARKER-001、WS-001 的有界任务并并行实施，避免把单项实验结束等同于整体目标完成。真实请求须有预算、证据及收尾。
- 规则层未改动：现有持续执行指令已覆盖，普通信号首次进入观察；不新增泛化规则，不将尚未完成的纯 Worker 目标写为已完成。
### EVO-003

- 用户明确纠正：“我说的是测速worker到webshare”。前轮本机HTTPS失败报告不回答Worker到Webshare延迟问题。
- 当次纠正：明确测量源/目的/指标，完成独立Cloudflare TCP测速Worker并真实测得SIN三节点174/175/234ms；本机结果与UI模拟数据分别标注，未冒认现有账号DO结果。见docs/tasks/WEBSHARE-LATENCY-001.md。
- 首次普通信号仅观察，规则层未修改；保留既有EVO-001/002内容，不把临时网络或宿主故障单独记为规则缺陷。